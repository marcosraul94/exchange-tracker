{
  "name": "exchange-tracker",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:lib": "npm run build -w @repo/lib",
    "build:scraper": "npm run build:lib && npm run build -w @repo/scraper",
    "build:scheduler": "npm run build:lib && npm run build -w @repo/scheduler"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.2"
  }
}
