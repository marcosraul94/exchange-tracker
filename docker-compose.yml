services:
  scraper:
    build:
      context: ./backend/scraper
    container_name: scraper
    entrypoint: /bin/sh
    volumes:
      - ./backend/scraper:/var/task/
    env_file:
      - .env
    environment:
      - NODE_ENV=dev
    stdin_open: true
    tty: true
